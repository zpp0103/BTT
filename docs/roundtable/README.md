# Roundtable development notes

This directory contains the deterministic, paper-first roundtable implementation.

The next UI iteration will expose four visual cards (strategy, confirmation, risk
veto, and final summary), equity/drawdown charts, and a signal timeline. The
backtest pipeline is intentionally completed first so the dashboard displays real
results instead of mocked performance.
